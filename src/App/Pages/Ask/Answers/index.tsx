import React, { FC } from 'react'
import styled from 'styled-components'

const Layout = styled.div`
  padding: 1rem;

  background-color: white;
`

export const Answers: FC = () => <Layout>Answers</Layout>
